import React from 'react'

const PeoplesData = ({name, height, mass, gender}) => {
  return (
    <div>
      <p>{name}</p>
      <p>{name}</p>
      <p>{name}</p>
      <p>{name}</p>
    </div>
  )
}

export default PeoplesData